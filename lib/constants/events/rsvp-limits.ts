/** Default number of additional guests allowed when an event enables plus-ones. */
export const DEFAULT_MAX_ADDITIONAL_GUESTS_PER_RSVP = 5;

/** Hard upper bound an organizer can set for additional guests on a single RSVP. */
export const MAX_ADDITIONAL_GUESTS_PER_RSVP = 10;

/** Max number of custom RSVP questions an organizer can add. Safe to use in client components. */
export const MAX_CUSTOM_RSVP_QUESTIONS = 5;

/** Max number of options a typed custom RSVP question can include. */
export const MAX_CUSTOM_QUESTION_OPTIONS = 5;

/** Max length for long-form RSVP answers. */
export const MAX_TEXTAREA_ANSWER_LENGTH = 5000;

/** Max number of attendee categories an organizer can configure for RSVP summaries. */
export const MAX_ATTENDEE_CATEGORIES = 8;
